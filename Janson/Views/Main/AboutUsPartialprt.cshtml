@using Janson.Models.Entity
@model List<AboutpartTbl>


@*@{
        var shortedData = Model.OrderBy(x => x.ID).ToList();
    }

    @for (int i = 0; i < shortedData.Count; i += 3)
    {
        @for (int j = i; j < i + 3 && j < shortedData.Count; j++)
        {*@


<div class="row pt-40">

    @foreach (var x in Model)
    {
        <div class="col-lg-3 col-md-4 col-sm-6">
            <div class="experience">
                <span>@x.Baslik</span>
                <p style="color:white">@x.icerik</p>
            </div>
        </div>

    }




</div>
